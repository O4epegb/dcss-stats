{
  "name": "@dcss-stats/root",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "prepare": "husky",
    "dev": "turbo dev",
    "build": "turbo build",
    "format": "turbo format",
    "lint": "turbo lint",
    "test": "turbo test"
  },
  "devDependencies": {
    "husky": "^9.0.11",
    "lint-staged": "^15.2.2",
    "turbo": "^2.0.4"
  },
  "lint-staged": {
    "*.{ts,tsx}": [
      "eslint --max-warnings 0"
    ],
    "*": [
      "prettier --write --ignore-unknown"
    ]
  },
  "packageManager": "yarn@4.2.2"
}
